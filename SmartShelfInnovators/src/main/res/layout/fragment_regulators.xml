<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical">

            <!-- DELI Card -->
            <TextView
                android:id="@+id/regulators_text_view"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/rounded_corners"
                android:padding="38dp"
                android:textColor="@android:color/black"
                android:textSize="30sp"
                android:textStyle="bold" />

            <include
                android:id="@+id/deli_card"
                layout="@layout/cardview_deli" />

            <!-- BAKERY Card -->
            <include
                android:id="@+id/bakery_card"
                layout="@layout/cardview_bakery" />

            <!-- DRINKS Card -->
            <include
                android:id="@+id/drinks_card"
                layout="@layout/cardview_drinks" />

            <!-- PRODUCE Card -->
            <include
                android:id="@+id/produce_card"
                layout="@layout/cardview_produce" />

            <!-- ICE CREAM Card -->
            <include
                android:id="@+id/icecream_card"
                layout="@layout/cardview_icecream" />

            <!-- DAIRY Card -->
            <include
                android:id="@+id/dairy_card"
                layout="@layout/cardview_dairy" />

        </LinearLayout>
    </ScrollView>
</androidx.coordinatorlayout.widget.CoordinatorLayout>
